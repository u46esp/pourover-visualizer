/**
 * Fired on `window` when the V60 switch drain control moves to the open (draining) state.
 * Listen with `window.addEventListener(V60_SWITCH_DRAIN_TOGGLED_ON, handler)`.
 */
export const V60_SWITCH_DRAIN_TOGGLED_ON = "V60_SWITCH_drainToggledOn" as const
