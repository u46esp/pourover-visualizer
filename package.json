{
    "name": "pourover-sim",
    "private": true,
    "version": "0.0.1",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview",
        "deploy": "npm run build && gh-pages -d dist"
    },
    "dependencies": {
        "three": "^0.160.0"
    },
    "devDependencies": {
        "gh-pages": "^6.3.0",
        "typescript": "^5.0.0",
        "vite": "^5.0.0"
    }
}
